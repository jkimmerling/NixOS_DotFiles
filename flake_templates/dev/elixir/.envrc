# Automatically load the Nix development shell
use flake

# Load .env file if it exists (for secrets, API keys, etc.)
dotenv_if_exists .env

# Custom project-specific environment variables
# export DATABASE_URL="postgresql://postgres:postgres@localhost:5432/myapp_dev"
# export SECRET_KEY_BASE="your-secret-key-here"
